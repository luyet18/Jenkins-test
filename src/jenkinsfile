pipeline {
  agent any

  tools {
    nodejs "node"
  }

  stages {
    stage('install') {
      steps {
        git branch: 'master', url: 'https://github.com/luyet18/Jenkins-test.git'
        dir('src') {
          sh 'npm install'
        }
      }
    }

  }

}