pipeline {
  agent any

  tools {
    nodejs "node"
  }

  stages {
    stage('install') {
      steps {
        git branch: 'master', url: 'https://github.com/luyet18/Jenkins-test.git'
        sh 'npm install'
        //dir('src') {
        //  sh 'npm install'
        //}
      }
    }

  }

}