pipeline {
  agent any

  tools {
    nodejs "node"
  }

  stages {
    stage('install') {
      steps {
        git branch: 'master', url: 'https://github.com/luyet18/Jenkins-test.git'
        dir('frontend') {
         bat 'npm install'
        }
      }
    }

  }

}